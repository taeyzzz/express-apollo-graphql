const Mutation = require("../domains/Mutation/types")
const Query = require("../domains/Query/types")
const Post = require("../domains/Post/types")
const User = require("../domains/User/types")

module.exports = [
  Query,
  Post,
  User,
  Mutation,
]
